FROM python:3.9-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install -r requirements.txt

COPY src/dashboard/ ./

EXPOSE 8503

CMD ["streamlit", "run", "executive_dashboard.py", "--server.port=8503", "--server.address=0.0.0.0"]